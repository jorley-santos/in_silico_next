**Pipeline Improvements - Incoming**

- Add parameter to control the number of simulated samples

- Add parameter to control the number of reads per simulated sample (default 250k reads as of now)

- Update utils/csv_samplesheet_maker.py to receive an input folder from the command line. The 
script should also check for the existence of the input folder.
